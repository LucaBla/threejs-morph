{
  "dependencies": {
    "gh-pages": "^6.0.0",
    "three": "^0.157.0"
  },
  "devDependencies": {
    "vite": "^4.5.0"
  },
  "scripts": {
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist"
  }
}
